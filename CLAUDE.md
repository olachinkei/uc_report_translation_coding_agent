# CLAUDE.md

Use `AGENTS.md` as the canonical agent entry point for this repository.

Before finalizing changes, run:

```bash
make check
```

Claude-specific hooks may call the same commands defined in `Makefile`.
